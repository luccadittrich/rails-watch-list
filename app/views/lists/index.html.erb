<p>List index</p>
<table>
  <% @lists.each do |l| %>
    <tr class="">
      <td>
        <%= link_to l.name, list_path(l)%>
      </td>
      <td>
        <%= link_to list_path(l), data: { method: :delete, confirm: "are you sure?" } do %>
          <%= render 'shared/trash'%>
        <% end %>
      </td>
      <td>
        <%= link_to edit_list_path(l) do %>
          <i class="fas fa-edit"></i>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
<%= link_to "new list", new_list_path, class:"btn btn-success" %>
<%= link_to "cards", cards_path, class:"btn btn-primary" %>
